# Adapter: codex

Nicht verifiziert. Auf der Maschine, auf der dieses Kit gebaut wurde, war kein `codex`
installiert (`command -v codex` → not found, geprueft 2026-09-09).

## 1. Session starten

`UNKNOWN — Startbefehl nicht geprueft. Beim Owner erfragen oder auf einer Maschine mit
installiertem Codex nachsehen und hier eintragen.`

## 2. Rollendatei laden

Codex liest `AGENTS.md` als Projektvertrag — dieses Repo legt ihn genau dort ab, das passt
ohne Zutun. Wie eine **Rollendatei** zusaetzlich als erster Prompt uebergeben wird:
`UNKNOWN — zu pruefen unter adapters/codex/, sobald der Startbefehl feststeht.`

## 3. Session-Kennung

`UNKNOWN — Sessionkennung nicht geprueft.`

## 4. Tokenbudget

`UNKNOWN — Ort und Format der Transkripte nicht geprueft.` Bis dahin gilt die Notbremse:
Ruhestand nach `KIT_MAX_TICKETS` Tickets.
