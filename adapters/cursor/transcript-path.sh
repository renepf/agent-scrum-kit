#!/usr/bin/env bash
# UNKNOWN — Ort und Format der cursor-Transkripte nicht geprueft.
# Scheitern ist hier die richtige Antwort: budget.md traegt UNKNOWN ein und die
# Rolle faellt auf die Notbremse (Ruhestand nach KIT_MAX_TICKETS Tickets) zurueck.
echo "UNKNOWN — keine Transkripte fuer cursor bekannt. Zu pruefen unter ~/.cursor/" >&2
exit 1
