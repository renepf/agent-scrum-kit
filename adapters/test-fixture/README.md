# Adapter: test-fixture

Kein echter Host. Nur fuer die Eval-Suite: er zeigt auf aufgezeichnete Transkripte unter
`evals/fixtures/` mit bekannten Zahlen. So laesst sich pruefen, ob `bin/budget.sh` den
Kontextwert richtig bildet, ohne einen Modellaufruf zu machen.
